diff --git a/backend/internal/infrastructure/repository/usage_log_repo_dashboard.go b/backend/internal/infrastructure/repository/usage_log_repo_dashboard.go
index 98785f52f..3e35e7a2a 100644
--- a/backend/internal/infrastructure/repository/usage_log_repo_dashboard.go
+++ b/backend/internal/infrastructure/repository/usage_log_repo_dashboard.go
@@ -4,6 +4,7 @@ import (
 	"context"
 	"errors"
 	"fmt"
+	"log/slog"
 	"time"
 
 	"github.com/Wei-Shaw/sub2api/internal/application/service"
@@ -388,6 +389,17 @@ func (r *usageLogRepository) GetUserDashboardStats(ctx context.Context, userID i
 	//   1) 无平台归属的极少数行（group/account 都没 platform）会被 HAVING 排除；
 	//   2) usageLogSuccessFilterUL 会把 actual_cost = 0 的失败 placeholder 行排除，
 	//      而 totalStatsQuery/todayStatsQuery 没有这层过滤、会把这些行的 request 计数算进去。
+	if err := r.loadUserDashboardPlatformStats(ctx, stats, userID, today); err != nil {
+		// Platform details are an optional projection. Keep the core dashboard
+		// usable when an older/partially migrated database cannot serve this
+		// additional join or when the query is cancelled under load.
+		slog.Warn("user dashboard platform stats unavailable", "user_id", userID, "error", err)
+	}
+
+	return stats, nil
+}
+
+func (r *usageLogRepository) loadUserDashboardPlatformStats(ctx context.Context, stats *UserDashboardStats, userID int64, today time.Time) error {
 	platformQuery := `
 		SELECT
 			` + usageLogEffectivePlatformExpr + ` as platform,
@@ -408,8 +420,9 @@ func (r *usageLogRepository) GetUserDashboardStats(ctx context.Context, userID i
 	`
 	rows, err := r.sql.QueryContext(ctx, platformQuery, userID, today)
 	if err != nil {
-		return nil, err
+		return err
 	}
+	platformStats := make([]PlatformDashboardStats, 0)
 	for rows.Next() {
 		var p PlatformDashboardStats
 		if err := rows.Scan(
@@ -422,18 +435,19 @@ func (r *usageLogRepository) GetUserDashboardStats(ctx context.Context, userID i
 			&p.TodayActualCost,
 		); err != nil {
 			_ = rows.Close()
-			return nil, err
+			return err
 		}
-		stats.ByPlatform = append(stats.ByPlatform, p)
-	}
-	if err := rows.Close(); err != nil {
-		return nil, err
+		platformStats = append(platformStats, p)
 	}
 	if err := rows.Err(); err != nil {
-		return nil, err
+		_ = rows.Close()
+		return err
 	}
-
-	return stats, nil
+	if err := rows.Close(); err != nil {
+		return err
+	}
+	stats.ByPlatform = platformStats
+	return nil
 }
 
 // getPerformanceStatsByAPIKey 获取指定 API Key 的 RPM 和 TPM（近5分钟平均值）
diff --git a/backend/internal/infrastructure/repository/usage_log_repo_dashboard_query_test.go b/backend/internal/infrastructure/repository/usage_log_repo_dashboard_query_test.go
index 8d68e5b90..5d8816a43 100644
--- a/backend/internal/infrastructure/repository/usage_log_repo_dashboard_query_test.go
+++ b/backend/internal/infrastructure/repository/usage_log_repo_dashboard_query_test.go
@@ -4,6 +4,7 @@ package repository
 
 import (
 	"context"
+	"errors"
 	"regexp"
 	"testing"
 	"time"
@@ -122,3 +123,35 @@ func TestFillSubjectDashboardUsageStatsRejectsUnknownColumn(t *testing.T) {
 	err := repo.fillSubjectDashboardUsageStats(context.Background(), &usagestats.UserDashboardStats{}, "unsafe", 1, time.Now())
 	require.ErrorContains(t, err, "unsupported dashboard subject column")
 }
+
+func TestGetUserDashboardStatsKeepsCoreStatsWhenPlatformQueryFails(t *testing.T) {
+	db, mock, err := sqlmock.New(sqlmock.QueryMatcherOption(sqlmock.QueryMatcherRegexp))
+	require.NoError(t, err)
+	t.Cleanup(func() { _ = db.Close() })
+
+	mock.ExpectQuery(`(?s)SELECT\s+COUNT\(\*\) AS total_api_keys.*FROM api_keys`).
+		WithArgs(int64(7), service.StatusActive).
+		WillReturnRows(sqlmock.NewRows([]string{"total_api_keys", "active_api_keys"}).AddRow(2, 1))
+	mock.ExpectQuery(`(?s)SELECT\s+COUNT\(\*\) AS total_requests.*FROM usage_logs\s+WHERE user_id = \$1`).
+		WithArgs(int64(7), sqlmock.AnyArg()).
+		WillReturnRows(sqlmock.NewRows([]string{
+			"total_requests", "total_input_tokens", "total_output_tokens", "total_cache_creation_tokens", "total_cache_read_tokens",
+			"total_cost", "total_actual_cost", "avg_duration_ms", "today_requests", "today_input_tokens", "today_output_tokens",
+			"today_cache_creation_tokens", "today_cache_read_tokens", "today_cost", "today_actual_cost",
+		}).AddRow(3, 30, 20, 0, 0, 2.5, 2.0, 100.0, 1, 10, 5, 0, 0, 0.8, 0.6))
+	mock.ExpectQuery(`(?s)SELECT\s+COUNT\(\*\) as request_count.*FROM usage_logs\s+WHERE created_at >= \$1 AND user_id = \$2`).
+		WithArgs(sqlmock.AnyArg(), int64(7)).
+		WillReturnRows(sqlmock.NewRows([]string{"request_count", "token_count"}).AddRow(0, 0))
+	mock.ExpectQuery(`(?s)SELECT\s+CASE WHEN g\.platform = 'composite'.*FROM usage_logs ul`).
+		WithArgs(int64(7), sqlmock.AnyArg()).
+		WillReturnError(errors.New("column groups.platform does not exist"))
+
+	repo := &usageLogRepository{sql: db}
+	stats, err := repo.GetUserDashboardStats(context.Background(), 7)
+	require.NoError(t, err)
+	require.Equal(t, int64(2), stats.TotalAPIKeys)
+	require.Equal(t, int64(3), stats.TotalRequests)
+	require.Equal(t, int64(50), stats.TotalTokens)
+	require.Empty(t, stats.ByPlatform)
+	require.NoError(t, mock.ExpectationsWereMet())
+}
