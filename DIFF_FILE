Quality inspection and account health inspection use separate settings, state keys,
services, schedulers, handlers and pages. The quality probe deadline changed from
30 seconds to an administrator-controlled timeout_seconds (default 120, range
30-300); timeout rows show error status without changing quality counters. The
admin quality page links to /monitor/quality/public and edits timeout_seconds.
