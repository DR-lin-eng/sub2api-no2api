Quality monitoring now runs two administrator-controlled stages independently: the
stage 1 candy question requires exact configured answer 21 and records reasoning
tokens; stage 2 sends the SVG pelican-bike prompt to the renderer/classifier.
Only wrong answers/low tokens/classifier `unnormal` increment degradation; transport
errors stay error. Source-group selection, degraded-group switching, public page,
configurable 30–300 second per-stage timeout, token buckets and legacy migration are
preserved.
